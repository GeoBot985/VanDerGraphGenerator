# App Icon Placeholder

This file marks the location for the packaged desktop application icon.

- Replace with `app_icon.ico` when the final icon asset is available.
- Keep the icon in `assets/icons/` so packaging can bundle it consistently.
