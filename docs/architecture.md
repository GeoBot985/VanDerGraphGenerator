# Architecture

User request -> LLM semantic mapping -> Neutral visual plan -> Deterministic validation -> Renderer adapter -> Deterministic output

This repository is a scaffold for a local-first architecture. The current sprint only defines the boundaries between subsystems.
